# Contributing

1. Keep the core idea: **no cross-vendor API/CLI driving**.
2. Prefer small, reviewable PRs.
3. Run `make test` before pushing (Python 3.11+ for the optional listener tests).
   `make test-basico` checks only the original Bash CLI.
4. Do not commit secrets, API keys, or private product paths.
